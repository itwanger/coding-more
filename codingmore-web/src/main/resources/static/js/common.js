// 页面公共ready事件
$(function() {
  // NProgress.start()
  window.addEventListener('load',() => {
    // setTimeout(()=>{
    //   NProgress.done()
    // })
  })
})

function OpenNewPage(url) {
  window.open(url)
}
